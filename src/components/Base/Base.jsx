import React from 'react';
import classes from './Base.module.css';

function Base(){
    return (
        <section className={ classes.main }>
            <h1>
                Hello World!
            </h1>
        </section>
    );
}
export default Base;